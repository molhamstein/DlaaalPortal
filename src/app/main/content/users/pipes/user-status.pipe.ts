/*
import { UserStatus } from './../enums/user-status';
import { Pipe, PipeTransform } from '@angular/core';

@Pipe({
  name: 'userStatusToBoolPipe'
})
export class UserStatusToBoolPipe implements PipeTransform {

  transform(value: String): any {
    return value == UserStatus.Active.toString() ? true : false;
  }

}
*/
